<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.dao.UserDao">
    <resultMap id="users" type="User">
        <id column="uid" property="uid"></id>
        <result column="username" property="username"></result>
        <result column="password" property="password"></result>
        <result column="name" property="name"></result>
        <result column="birthday" property="birthday"></result>


        <!--Collection:映射accountList 属性
              property :对应的属性名
              ofType：集合中的元素类型：与association中的javaType效果一样

              uid：应为accout表中u_id 关联的是users 表中的uid
          -->
        <collection property="accountList" ofType="account" column="uid" select="com.itheima.dao.AccountDao.findByUserId">

        </collection>
    </resultMap>
    
    
    <select id="findAll" resultMap="users">
      select * from users
    </select>
</mapper>